site:
  server: # each line is a partition, the first is the master site_name:port
    - ["s101:11001", "s201:11002", "s301:11003"]
  client: # each line is a partition
    - ["c101", "c201", "c301", "c401", "c501"]

# process_name - host_addr map
process:
  s101: localhost
  s201: localhost
  s301: localhost
  c101: localhost
  c201: localhost
  c301: localhost
  c401: localhost
  c501: localhost
    
host:
  localhost: 127.0.0.1

